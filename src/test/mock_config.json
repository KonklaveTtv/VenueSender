{
	"email_pass_encrypted" : false,
	"email_password" : "enter_email_password",
	// Sender's email address and SMTP port
	"sender_email" : "your_sender_email@example.com", // do not change, this is for tests
	"sender_smtp_port" : 587, // do not change, this is for tests
	"smtp_pass_encrypted" : false,
	"smtp_password" : "enter_smtp_password",
	"smtp_port" : 587, // do not change, this is for tests
	// SMTP Server settings
	"smtp_server" : "mock_smtp_server", // do not change, this is for tests
	// SMTP Authentication credentials
	"smtp_username" : "mock_smtp_username", // do not change, this is for tests
	// Path to the Test Mock CSV file containing venue data
	"venues_csv_path" : "mock_venues.csv" // do not change, this is for tests
}